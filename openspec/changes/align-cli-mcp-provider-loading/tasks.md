## 1. CLI provider registration

- [x] 1.1 Align CLI tool, resource, and prompt token loading with the `mcp` settings contract.
- [x] 1.2 Preserve token-derived names for CLI resource and prompt registrations.

## 2. Validation

- [x] 2.1 Add CLI factory coverage for configured tool, resource, and prompt providers.
- [x] 2.2 Run the focused `@tsed/platform-mcp` tests and type checks.
- [x] 2.3 Align CLI transports with the shared MCP v2 server and validate package type checks.

## 3. Documentation

- [x] 3.1 Document standalone CLI MCP server setup and transports in the MCP guide.
