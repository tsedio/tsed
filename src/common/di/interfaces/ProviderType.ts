export enum ProviderType {
    FACTORY = "factory",
    SERVICE = "service",
    PROVIDER = "provider",
    CONVERTER = "converter",
    INTERCEPTOR = "interceptor",
    FILTER = "filter",
    MIDDLEWARE = "middleware"
}