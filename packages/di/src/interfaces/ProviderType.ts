export enum ProviderType {
  VALUE = "value",
  FACTORY = "factory",
  SERVICE = "service",
  PROVIDER = "provider",

  // NOT STANDARD
  CONTROLLER = "controller",
  CONVERTER = "converter",
  INTERCEPTOR = "interceptor",
  FILTER = "filter",
  MIDDLEWARE = "middleware"
}
