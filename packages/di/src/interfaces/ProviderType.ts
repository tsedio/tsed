export enum ProviderType {
  VALUE = "value",
  FACTORY = "factory",
  SERVICE = "service",
  PROVIDER = "provider",
  CONTROLLER = "controller",
  INTERCEPTOR = "interceptor",

  // NOT STANDARD
  CONVERTER = "converter",
  FILTER = "filter",
  MIDDLEWARE = "middleware"
}
