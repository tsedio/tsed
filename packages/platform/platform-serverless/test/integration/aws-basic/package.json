{
  "name": "@tsed/aws-basic",
  "scripts": {
    "build": "microbundle --target node --no-compress --format modern,cjs --tsconfig ./tsconfig.compile.json",
    "start":  "serverless offline start",
    "invoke": "serverless invoke local -f timeslots"
  },
  "dependencies": {
    "@tsed/di": "^6.69.2",
    "@tsed/schema": "^6.69.2",
    "@tsed/platform-serverless": "^6.69.2",
    "microbundle": "0.13.0",
    "serverless-offline": "^8.2.0"
  },
  "devDependencies": {

  }
}
