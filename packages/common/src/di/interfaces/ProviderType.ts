export enum ProviderType {
  FACTORY = "factory",
  SERVICE = "service",
  PROVIDER = "provider",
  CONTROLLER = "controller",
  CONVERTER = "converter",
  INTERCEPTOR = "interceptor",
  FILTER = "filter",
  MIDDLEWARE = "middleware"
}
