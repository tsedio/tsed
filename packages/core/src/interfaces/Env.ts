/**
 *
 */
export enum Env {
  PROD = "production",
  DEV = "development",
  TEST = "test"
}

/**
 * Fallback
 * @type {Env}
 */
// tslint:disable-next-line: variable-name
export const EnvTypes = Env;
